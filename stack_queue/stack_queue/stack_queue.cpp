#include <deque>
using namespace std;
#include "stack.h"
#include "Queue.h"
int main()
{
	//test_stack();
	test_Queue();
	deque<int> dq;
	dq.push_back(1);
	dq.push_back(2);
	dq.push_back(3);
	dq.push_back(4);
	dq.push_back(5);
	while (!dq.empty())
	{
		cout<<dq.
	}

	return 0;
}